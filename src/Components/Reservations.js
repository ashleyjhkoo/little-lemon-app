import './Reservations.css';

const Reservations = ({ className }) => {
    return (
        <main className="bg-container_reservations">
            <section className="content-container_reservations">
                <h1 id="reservations-title">Reservations</h1>
                <p>This is the placeholder page for the Reservations menu.</p>
            </section>
        </main>
    );
};

export default Reservations; 